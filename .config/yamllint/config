extends: default

rules:
  comments:
    level: error
  comments-indentation:
    level: error
  document-start: disable
  empty-lines: disable
  empty-values: disable
  line-length: disable
  quoted-strings:
    required: only-when-needed
  truthy:
    level: error
    check-keys: false
